version: '3.8'

services:
  localstack:
    image: localstack/localstack:stable
    container_name: jreg-localstack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3
      - DEBUG=1

volumes:
  localstack-data:
