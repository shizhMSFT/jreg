spring:
  application:
    name: jreg

aws:
  s3:
    bucket-name: jreg-registry
    endpoint: http://localhost:4566
    region: us-east-1
    access-key: test
    secret-key: test

logging:
  level:
    com.jreg: DEBUG
    com.amazonaws: WARN
